import React, { Component } from "react";
import { View, Text } from "react-native";

import styles from "./styles";

export default class ContactScreen extends Component {

    render() {
        return(
            <View>
                <Text>Página ContactScreen</Text>
            </View>
        );
    }
}
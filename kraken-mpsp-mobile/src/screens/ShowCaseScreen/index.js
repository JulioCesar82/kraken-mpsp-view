import React, { Component } from "react";
import { View, Text } from "react-native";

import styles from "./styles";

export default class ShowCaseScreen extends Component {

    render() {
        return(
            <View>
                <Text>Página ShowCaseScreen</Text>
            </View>
        );
    }
}